# Production/Heroku Database Configuration
# JawsDB MySQL connection
spring.datasource.url=jdbc:mysql://gzp0u91edhmxszwf.cbetxkdyhwsb.us-east-1.rds.amazonaws.com:3306/na9t89qhy0p328pc
spring.datasource.username=xnho8lw1tvhttapm
spring.datasource.password=v7d9gws99qktv5yw
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration for Production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false

# MySQL specific settings
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000

# Server Configuration
server.port=${PORT:8080}

# Production logging
logging.level.root=WARN
logging.level.com.example.demo=INFO

# Disable H2 console in production
spring.h2.console.enabled=false

# =================================
# OAUTH2 FOR PRODUCTION (HEROKU)
# =================================
# GitHub OAuth2 Provider - enabled in production
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET}
spring.security.oauth2.client.registration.github.scope=read:user,user:email

# Google OAuth2 Provider - enabled in production
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
