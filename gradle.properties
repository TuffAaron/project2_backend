# Gradle configuration for better build performance and Heroku compatibility
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true

# Skip tests for Heroku deployment if needed
# Can be overridden with -PskipTests=false
skipTests=false
